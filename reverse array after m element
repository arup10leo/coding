#include <bits/stdc++.h> 
void reverseArray(vector<int> &arr , int m)
{
	vector<int>v = arr;
	int n = v.size()-1;
	int r=0;
	for(int i=0;i<n;i++){
		if(i<(n-r)and i>m){
            swap(arr[i],arr[n-r]);
			r++;
		}
	}
}
